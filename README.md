bh-tournament
=============

* `npm install`.
* `npm run start-dev`.
* `localhost:8080`.

# Set config
* `npm run env`.
* Edit your `.env` file in the root directory.

NOTE: npm start will not be able to start the backend right now.
